import React from "react";

const UserContext = React.createContext();

export default UserContext;

//Here we create a Context and store in a variable UserContext and then export

//here we use .js file because there is no need to .jsx file
